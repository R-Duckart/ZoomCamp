[project]
name = "07-stream"
version = "0.1.0"
description = "Use RedPanda to stream data with Kafka protocol"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kafka-python>=2.3.1",
    "pandas>=3.0.2",
    "pyarrow>=24.0.0",
    "avro-python3>=1.10.2",
    "confluent-kafka[avro]>=2.3.0",
    "pydantic>=2.13.3",
]
